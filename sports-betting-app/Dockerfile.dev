FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY . .

EXPOSE 3000 5173

# Dev: run Express backend + Vite dev server with --host
CMD ["npx", "concurrently", "node server.js", "npx vite --host"]
